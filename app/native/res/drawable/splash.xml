<?xml version="1.0" encoding="utf-8"?>
<!-- The splash the app holds up until it is ready (@capacitor/splash-screen, launchAutoHide false). The same
     picture Android 12 shows on its own, so nothing on screen changes when one hands over to the other:
     288dp is the size windowSplashScreenAnimatedIcon is drawn at. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background"/>
    <item
        android:drawable="@drawable/splash_logo"
        android:gravity="center"
        android:width="288dp"
        android:height="288dp"/>
</layer-list>
