# Exact versions, so that a session on the web, the CI and a release build all run the same tools.
# playwright has to match the ready-made Chromium of Claude Code on the web (build 1194, see
# .claude/hooks/session-start.sh), which is why it is pinned to the patch level like everything else.
pillow==12.3.0
playwright==1.56.0
ruff==0.15.8
