#!/bin/sh
# Vor dem Entfernen den Dienst stoppen (bei einem Update startet postinst ihn neu)
set -e
if [ "$1" = remove ] && [ -d /run/systemd/system ]; then
  systemctl disable --now schmeckts.service >/dev/null 2>&1 || true
fi
exit 0
